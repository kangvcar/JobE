# 只对能力变更使用双时态，技能重要度按时间片分片存储

技能点在岗位上的重要度是每个时间片重新计算的聚合观测值，不是"在某段时间内为真的事实"，因此它没有有效时间语义，建成按时间片分片的加权边即可。真正需要双时态的只有能力变更这类元数据事件——技能点被合并、被重命名、被判定为噪声，或历史数据被回溯修正——这部分数据量很小，用业务发生时间与记录时间两个字段表达。

## Consequences

避免了给全图上双时态带来的查询复杂度与写入成本。回溯修正历史结论时，业务发生时间记原始时点、记录时间记修正时点，使"我们当时认为"与"我们现在认为"可以分别复原。
